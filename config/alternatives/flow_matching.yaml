# Apply after a dataset config with a second --config argument.
model:
  class_path: diffusion.GenerativeModel
  init_args:
    model_type: flow_matching
    trajectory: linear
    prediction_type: velocity
    sampling_scheduler: heun
    sampling_steps: 40
    norm: group
trainer:
  name: flow_matching
